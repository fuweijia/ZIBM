% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZIBM.R
\name{ZIBM}
\alias{ZIBM}
\title{The main function estimates direct and indirect effects and standard errors, and calculates unadjusted and adjusted p-values (Benjamini-Hochberg adjustment).}
\usage{
ZIBM(
  MicrobData,
  CovData,
  lib_name,
  y_name,
  x_name,
  conf_name,
  k_range,
  num_cores,
  zero_prop_NIE2 = 0.1,
  zero_count_NIE2 = 4 * (length(conf_name) + 2),
  x4_inter = TRUE,
  x5_inter = TRUE
)
}
\arguments{
\item{MicrobData}{Matrix/data frame of microbiome data (subjects x taxa).}

\item{CovData}{Data frame with outcome, exposure, library size, and
confounder columns, in the same subject order as MicrobData.}

\item{lib_name}{Column Name of library size variable within CovData.}

\item{y_name}{Column name in CovData for outcome Y.}

\item{x_name}{Column name in CovData for covariate of interest X.}

\item{conf_name}{Name of confounders within CovData. Default is NULL, meaning no confounder.}

\item{k_range}{Integer vector of candidate mixture-component counts to try per taxon; best k chosen by AIC.}

\item{num_cores}{Number of cores for parallel fitting.}

\item{zero_prop_NIE2}{Proportion-of-zeros threshold for using the zero-inflated variant. Default 0.1.}

\item{zero_count_NIE2}{Absolute zero-count threshold. Default 4 * (length(conf_name) + 2)/}

\item{x4_inter}{Whether to include the interaction term \mjeqn{\beta_4}{} Default is TRUE.}

\item{x5_inter}{Whether to include the interaction term \mjeqn{\beta_5}{} Default is TRUE.}
}
\value{
List with:
\describe{
\item{list_save}{A list per-taxon of res_fin_med (AIC-selected fit, or NA if all k failed) and
res_list_med (fits for every k).}
\item{nTaxa}{Number of taxa.}
\item{nSub}{Number of subjects.}
\item{taxon_ori_name}{Original taxon names.}
}
}
\description{
The main function estimates direct and indirect effects and standard errors, and calculates unadjusted and adjusted p-values (Benjamini-Hochberg adjustment).
}
\examples{
\donttest{
n_sub  <- 40
n_taxa <- 10
lib_size   <- sample(8000:12000, n_sub, replace = TRUE)
taxon_prop <- matrix(rbeta(n_sub * n_taxa, 0.5, 20), nrow = n_sub)
MicrobData <- matrix(
  rbinom(n_sub * n_taxa, size = lib_size, prob = taxon_prop),
  nrow = n_sub, ncol = n_taxa,
  dimnames = list(NULL, paste0("taxon", seq_len(n_taxa)))
)

CovData <- data.frame(
  Y   = rnorm(n_sub),
  X   = rbinom(n_sub, 1, 0.5),
  Z   = rnorm(n_sub),
  lib = lib_size
)

res <- ZIBM(
  MicrobData = MicrobData,
  CovData    = CovData,
  lib_name   = "lib",
  y_name     = "Y",
  x_name     = "X",
  conf_name  = "Z",
  k_range    = 1,
  num_cores  = 1
)
res_table <- summarize_results(ZIBM_res = res)
}
}
